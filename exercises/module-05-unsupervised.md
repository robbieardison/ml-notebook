# Module 5 exercise

Using `course/05-unsupervised-learning/05-clustering-and-pca.ipynb`:

1. Vary `n_clusters` in KMeans on the PCA 2D embedding; plot inertia vs k (elbow sketch).
2. For DBSCAN, try two `eps` values and describe how cluster count and noise change.
3. When would you **not** trust PCA for clustering?
