# Module 0 exercise

1. Create a virtual environment, install `requirements.txt`, and run `course/00-setup-and-prereqs/00-environment-and-jupyter.ipynb` top to bottom.
2. Confirm `utils.data_paths.data_dir()` prints a path ending in `/data`.
3. Write one paragraph: why should `StandardScaler` live inside a `Pipeline` when using cross-validation?
