# Module 2 exercise

Pick **one** notebook from `course/02-classification/` that uses a real CSV.

1. Print class balance for the target variable.
2. Compare **two** models with the **same** validation split; use at least two metrics (e.g. ROC-AUC + F1).
3. Write 3–5 bullets on **when** you would prefer SVM vs RandomForest for tabular data.
