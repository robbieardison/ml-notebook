# Module 7 exercise

1. Change the synthetic series in `07-baseline-forecasting.ipynb` to add a **level shift** halfway; observe forecast error.
2. Implement a **naive** baseline: predict the next month as the previous month; compare MAE to Holt-Winters.
3. Why is random `train_test_split` invalid for real forecasting?
