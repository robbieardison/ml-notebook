# Module 3 exercise

Open `course/03-model-evaluation-and-selection/03-cross-validation-and-metrics.ipynb`.

1. Change `scoring` to `average_precision` and `f1`; summarize how rankings differ from ROC-AUC.
2. Add **calibration** discussion: when is ROC-AUC misleading for deployment?
3. (Stretch) Wrap the search in `GridSearchCV` over `LogisticRegression.C`.
